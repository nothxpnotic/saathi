# SAATHI — Python Dependencies
# Install with:  pip install -r requirements.txt

# Core ML
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0

# Model persistence
joblib>=1.3.0

# Visualisation (evaluation plots)
matplotlib>=3.8.0
seaborn>=0.13.0

# Optional: ONNX export for Android deployment
# onnx>=1.15.0
# skl2onnx>=1.16.0

# UI (built-in with Python 3.x — listed for documentation only)
# tkinter  (included in standard library)
